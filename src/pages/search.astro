---
import BaseLayout from "../layouts/BaseLayout.astro";
import "../styles/pages/search.css";
import SearchFilters from "../components/search/SearchFilters";
import { formatMonthYear, getVisibleBossFeatures, getVisibleExcited, getVisibleReviews } from "../lib/content";

const [reviews, bossfeatures, excited] = await Promise.all([getVisibleReviews(), getVisibleBossFeatures(), getVisibleExcited()]);
const searchIndex = [...reviews, ...bossfeatures, ...excited]
  .map((entry) => ({
    title: entry.data.title,
    description: entry.data.description,
    blurb: entry.data.blurb ?? "",
    type: "game" in entry.data ? "Boss Feature" : "status" in entry.data ? "I'm Excited" : "Review",
    url: `/${"game" in entry.data ? "bossfeatures" : "status" in entry.data ? "im-excited" : "reviews"}/${entry.slug}/`,
    date: formatMonthYear(entry.data.pubDate),
    genres: (entry.data.genres ?? []).join(", "),
    platforms: (entry.data.platforms ?? []).map((p: { name: string }) => p.name).join(", "),
  }))
  .sort((a, b) => a.title.localeCompare(b.title));
---

<BaseLayout title="Search" description="Search reviews and boss features." mainClass="search-page">
  <section class="search-head" aria-labelledby="search-title">
    <p class="search-eyebrow">Kat's Kronicles / Index</p>
    <h1 class="search-title" id="search-title">Search the archive</h1>
  </section>

  <SearchFilters client:load searchIndex={searchIndex} />
</BaseLayout>
