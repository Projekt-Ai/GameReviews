const goty = {
  year: 2026,
  winner: null, // set to the exact title string when decided
  nominees: [
    {
      title: "Homura Hime",
      slug: "homura-hime-review-style-is-its-own-ceiling",
      thumbnail: null,   // fallback if no slug
      platform: null,    // fallback if no slug
      releaseDate: null, // fallback if no slug
      note: null,        // personal thoughts
    },
  ],
};

export default goty;
